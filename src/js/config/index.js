export default {
    character: {
        initialWidth: 80,
        initialHeight: 80,
        initialKeydownMoveSpeed: 10
    },
    bullet: {
        initialWidth: 10,
        initialHeight: 20,
        initialSpeed: 10,
        initialGenerateInterval: 60
    },
    enemy: {
        initialWidth: 60,
        initialHeight: 60,
        initialSpeed: 2,
        initialGenerateInterval: 15
    },
    award: {
        character: {
            initialWidth: 60,
            initialHeight: 60,
            initialSpeed: 2,
            initialGenerateInterval: 1200
        }
    }
};